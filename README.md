# Screen Nutrition

Jekyll site migrated from Squarespace using the Modernist theme.

## Local setup

```bash
bundle install
bundle exec jekyll serve
```

Pages are in `pages/` with permalinks matching the original Squarespace slugs.
